#include <ros/ros.h>

#include <control_node/states.h>

using namespace std;

int main(int argc, char *argv[])
{
	ros::init(argc, argv, "state_interrupt");
	//control_state = STATE_BROAD_SEARCH;
	return 0;
}